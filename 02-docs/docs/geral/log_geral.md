# Log Geral — MRP_LOCAL

## v0.1.037-saneamento

Base limpa preparada para retomada de desenvolvimento.

Ações registradas:

- `REGRAS_MRP.txt` incorporado como fonte absoluta.
- Configuração central criada em `01-mrp/config/mrp_local.env.json`.
- Scripts de serviço reestruturados para raiz resolvida/configurável.
- Watchdog refeito como processo contínuo.
- Logs reais removidos do pacote limpo.
- `Thumbs.db`, `.git`, `.codex`, quarentena pesada, snapshots e patches antigos removidos do pacote limpo.
- Manual operacional/dev em `.txt` criado.
- Resumo operacional em `.txt` criado.
- Gancho de empacotamento/instalador futuro registrado.

Status: pronto para retomada de desenvolvimento, mas ainda não homologado/blindado.

Pendente: validação operacional em Windows real.
