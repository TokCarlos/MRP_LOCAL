MRP_LOCAL - MANUAL DEV COMPLETO

Versao documental: v0.1.049-fix-launcher-painel-admin
Base anterior concluida: v0.1.049 painel admin local preparacao
Commit base relevante anterior: 173456b - chore: preparar portabilidade pos-cimasp v0.1.045

STATUS REAL

- Frontend validado pelo usuario: start OK, porta 8765 OK, healthcheck OK e sistema abrindo corretamente.
- Deploy no trabalho validado.
- Acesso no trabalho por outras maquinas da rede validado (script auxiliar).
- Acesso remoto de casa via Tailscale validado.
- Sistema ainda NAO esta blindado.
- Sistema ainda NAO esta homologado.
- Backend FastAPI ainda NAO existe.
- Banco real ainda NAO existe.
- PostgreSQL ainda NAO esta instalado/configurado pelo sistema.
- Python portable ainda NAO foi baixado/ativado pelo sistema.
- Painel Administrativo Local em preparacao (separado do index.html).
- Watchdog, tarefa automatica, reboot, queda de energia e rede ainda precisam validacao real.

REGRA OPERACIONAL

Antes de qualquer alteracao relevante, ler REGRAS_MRP.txt.

O sistema e local-first. Caminhos fisicos do ambiente atual podem aparecer em documentacao, config e scripts de ambiente, mas nao devem virar regra de negocio.

ESTRUTURA OFICIAL

Raiz do projeto em ambiente DEV atual: X:\

Pastas principais:

- 01-mrp: nucleo executavel do sistema.
- 02-docs: documentacao, regras, historico e auditoria.
- 03-vs: scripts, relatorios, patches, snapshots e releases.
- 00-manual-dev: documentos operacionais do dono/dev.

Estrutura preparada em 01-mrp:

- config: configuracoes do ambiente.
- runtime: reservado para Python/Node portatil futuro.
- tools: ferramentas auxiliares futuras.
- data: dados locais futuros do nucleo.
- db: reservado para SQLite/PostgreSQL futuro, sem banco real agora.
- logs: logs reais de execucao, nao versionar conteudo real.
- tmp: temporarios, nao versionar conteudo real.
- backups: backups reais, nao versionar conteudo real.
- front_end: frontend atual funcional.
- back_end: reservado para backend futuro.
- engine: reservado para motores de regra de negocio.
- adapters: reservado para adaptadores.
- install: prechecks e ganchos de instalador futuro.
- health: prechecks de saude do ambiente.
- docs_runtime: documentacao do nucleo transportavel.

FRONTEND ATUAL

Diretorio funcional:

01-mrp/front_end

Porta oficial:

8765

Bind operacional:

0.0.0.0

Operacao principal:

- MRP_MENU_SISTEMA.bat
- 03-vs/scripts/servicos/mrp_frontend_start.ps1
- 03-vs/scripts/servicos/mrp_frontend_stop.ps1
- 03-vs/scripts/servicos/mrp_frontend_status.ps1
- 03-vs/scripts/servicos/mrp_frontend_healthcheck.ps1

Comando py/http.server:

- uso somente para diagnostico tecnico pontual;
- nao usar como operacao principal.

Scripts atuais de servico:

- 03-vs/scripts/servicos/mrp_frontend_start.ps1
- 03-vs/scripts/servicos/mrp_frontend_stop.ps1
- 03-vs/scripts/servicos/mrp_frontend_status.ps1
- 03-vs/scripts/servicos/mrp_frontend_healthcheck.ps1
- 03-vs/scripts/servicos/mrp_frontend_watchdog.ps1
- 03-vs/scripts/servicos/mrp_frontend_task_install.ps1
- 03-vs/scripts/servicos/mrp_frontend_task_uninstall.ps1
- 03-vs/scripts/servicos/mrp_firewall_8765.ps1

Painel administrativo local:

- 03-vs/scripts/painel/mrp_painel_controle.py
- 03-vs/scripts/painel/mrp_admin_auth_setup.py
- launcher principal: MRP_PAINEL_SERVIDOR.vbs
- launcher diagnostico: MRP_PAINEL_SERVIDOR.cmd
- criar atalho: 03-vs/scripts/painel/criar_atalho_painel.ps1
- batch opcional: CRIAR_ATALHO_PAINEL_SERVIDOR.bat

Credencial local admin (nao versionar):

- 01-mrp/config/local/admin_auth.local.json

Estado local de modo automatico (nao versionar):

- 01-mrp/config/local/mrp_auto_mode.local.json

Comandos principais:

Iniciar:

powershell -NoProfile -ExecutionPolicy Bypass -File .\03-vs\scripts\servicos\mrp_frontend_start.ps1

Parar:

powershell -NoProfile -ExecutionPolicy Bypass -File .\03-vs\scripts\servicos\mrp_frontend_stop.ps1

Status:

powershell -NoProfile -ExecutionPolicy Bypass -File .\03-vs\scripts\servicos\mrp_frontend_status.ps1

Healthcheck:

powershell -NoProfile -ExecutionPolicy Bypass -File .\03-vs\scripts\servicos\mrp_frontend_healthcheck.ps1

PRECHECK DE PORTABILIDADE

Precheck de instalacao futura:

powershell -NoProfile -ExecutionPolicy Bypass -File .\01-mrp\install\mrp_install_precheck.ps1

Precheck de saude do nucleo:

powershell -NoProfile -ExecutionPolicy Bypass -File .\01-mrp\health\mrp_health_precheck.ps1

Esses scripts nao instalam nada, nao criam banco, nao iniciam backend e nao alteram dados reais.

ACESSO

Local no servidor:

http://localhost:8765

Rede local:

http://IP_LOCAL_DO_SERVIDOR:8765

Tailscale:

http://IP_TAILSCALE_DO_SERVIDOR:8765

Dados de teste operacional:

- IP LAN usado no script auxiliar: 192.168.1.71
- IP Tailscale usado no teste: 100.117.224.127
- Esses IPs nao sao regra fixa do sistema e devem ser revisados se o servidor mudar.

AREA PORTABLE (APOIO OPERACIONAL)

A pasta `portable` deve permanecer preservada como area oficial de deploy/teste/acesso externo.

Arquivo chave:

- portable\CONFIGURAR_ACESSO_MRP_REDE.bat

Uso:

- ferramenta operacional auxiliar para alinhar PCs clientes da rede;
- testa/acessa servidor MRP por LAN e/ou Tailscale;
- ajusta excecoes de proxy/bypass no perfil do usuario Windows;
- gera log local em `%LOCALAPPDATA%`;
- nao e core de regra de negocio.

Caminhos de trabalho validados:

- \\Departamentos\PCP\9_Sistema
- X:\9_Sistema quando X: aponta para \\Departamentos\PCP

DECISOES

- MEGA descontinuado.
- Tailscale sera usado para acesso remoto.
- Rede local e o acesso principal.
- Sistema deve ser local-first.
- Backend FastAPI fica para etapa futura.
- PostgreSQL fica para etapa futura.
- O sistema ainda nao deve ser chamado de blindado.

REGRAS DE VERSIONAMENTO

Antes de commit:

- validar encoding;
- validar lixo operacional;
- validar ausencia de logs reais versionaveis;
- validar ausencia de Thumbs.db;
- validar ausencia de banco real;
- validar documentacao minima;
- validar que frontend funcional nao foi alterado sem escopo.

CHECKLIST DE NOVA MAQUINA

1. Copiar ou clonar o projeto.
2. Conferir estrutura 01-mrp, 02-docs, 03-vs e 00-manual-dev.
3. Conferir Python ou runtime configurado.
4. Rodar precheck de instalacao.
5. Rodar precheck de saude.
6. Iniciar frontend.
7. Testar http://localhost:8765.
8. Liberar firewall 8765 quando necessario.
9. Testar acesso por IP local.
10. Testar Tailscale quando aplicavel.
11. Validar start/stop/status/healthcheck.
12. Somente depois avaliar tarefa automatica/watchdog/reboot.

PENDENCIAS

- Validar tarefa Windows em ambiente real.
- Validar reboot.
- Validar queda de energia.
- Validar queda de rede.
- Validar watchdog continuo.
- Corrigir ou justificar qualquer encoding legado restante.
- Separar pacote DEV de pacote RELEASE.
- Definir backend FastAPI futuro.
- Definir banco real futuro.

PLANEJAMENTO

- proxima etapa planejada: validacao operacional no DEV casa com painel admin local.
- etapa seguinte planejada: watchdog/tarefa automatica/reboot.

REGRA ESTRATEGICA V0.1.050 - INSTALADOR FUTURO

- Objetivo futuro: distribuicao por instalador unico de uso comum.
- Exemplo conceitual: MRP_LOCAL_Setup.exe.
- Nesta etapa: somente regra/documentacao. Sem implementacao.
- O instalador futuro deve preparar ambiente e arquivos, executar precheck e registrar pendencias por nivel:
  - CRITICO: bloqueia.
  - OPCIONAL: alerta e permite continuar quando nao bloquear operacao.
  - RECOMENDADO: alerta e permite continuar.
- Acoes sensiveis devem exigir aprovacao administrativa.
- O instalador futuro nao pode depender de X:\, \\HOME-MACHINE, usuario Windows fixo ou drive mapeado.
- Decisao de tecnologia do instalador fica para etapa futura.

REGRA ESTRATEGICA V0.1.050 - NOME AMIGAVEL/PROXY E IA FUTURA

- Objetivo futuro de acesso: esconder IP/porta do usuario final com nome amigavel.
- Exemplos conceituais: http://mrp.local, https://mrp.local, https://mrp-trabalho.<tailnet>.ts.net
- Porta 8765 continua interna/configuravel.
- Nome amigavel/proxy e infraestrutura, nao regra de negocio.
- Opcao futura LAN: hosts local em clientes, DNS interno e proxy reverso 80/443 para 127.0.0.1:8765.
- Opcao futura Tailscale: MagicDNS e Tailscale Serve com HTTPS.
- Nao implementar isso nesta etapa.

- Integracao IA futura deve ser via backend FastAPI, nunca direta no frontend com chave exposta.
- IA deve operar com ferramentas controladas, permissao, confirmacao em acao critica e log.
- IA nao deve executar PowerShell livre.
- IA nao deve acessar banco diretamente.
- Chaves/tokens ficam fora do Git.
- Nao implementar IA nesta etapa.

ETAPA V0.1.051 - BACKEND PRODUTOS (BASE TECNICA)

- Produto e o cadastro-mestre de aparelhos/equipamentos.
- Nesta etapa foi criada base tecnica de backend para validar seed e contrato.
- Sem servidor FastAPI em execucao.
- Sem banco real.
- Sem instalacao de dependencias.
- Fonte atual continua `01-mrp/front_end/data/produtos_seed.json`.
- Imagens continuam em arquivo fisico com referencia por caminho.

REGISTRO FUTURO - MODELO PROGRAMA WINDOWS

- O repositorio DEV nao e pasta de programa instalado.
- Build/release futuro pode conter executaveis e bibliotecas de runtime.
- Exemplo conceitual de release:
  - MRP_LOCAL_Setup.exe
  - MRP_Server.exe
  - MRP_Painel.exe
  - runtime/
  - app/
  - config/
  - data/
  - logs/
  - db/
- `.dll`, `.pyd` e `.bin` sao esperados apenas no artefato de release quando a etapa de empacotamento for aberta.

ETAPA V0.1.050 - ICONS DLL LAUNCHER

- etapa historica de recurso visual;
- DLL nao e padrao atual do atalho.

ETAPA V0.1.052 - ICONES OFICIAIS .ICO

- correcao principal: usar `.ico` direto no atalho da Area de Trabalho;
- icones oficiais gerados em `01-mrp/assets/icons/windows/ico`;
- padrao do painel: `mrp_mrp_dark.ico`;
- fallback: `mrp_pcp_light.ico`;
- DLL permanece opcional/futura, nao padrao do atalho.

CORRECAO FINAL DE ATALHO VISUAL

- nao tratar `.vbs` como executavel visual com icone proprio;
- item visual oficial e o atalho `.lnk` na Area de Trabalho;
- `TargetPath` do atalho deve ser `wscript.exe`;
- `.vbs` deve entrar em `Arguments`;
- icone oficial fica no `.lnk` com arquivo `.ico` local em `%LOCALAPPDATA%\MRP_LOCAL\icons`.

SANEAMENTO V0.1.052b

- mantidos como oficiais os 4 icones .ico atuais;
- removidos artefatos antigos de DLL sem uso operacional;
- limpeza de lixo DEV (__pycache__, pyc, logs, zip transitorio);
- criado gerador de pacote limpo para upload/revisao.

ETAPA V0.1.053 - BACKEND MINIMO + API PRODUTOS

Problema:
- frontend Produtos dependia de seed direto e o sistema ainda nao tinha backend local minimo para saude/status/api de produtos.

Causa:
- etapas anteriores mantiveram somente base preparatoria sem servidor API em execucao.

Solucao:
- consolidar backend minimo FastAPI em `01-mrp/back_end/app` com:
  - `GET /health`
  - `GET /api/status`
  - `GET /api/produtos`
- criar scripts operacionais backend (start/status/healthcheck/stop) em `03-vs/scripts/servicos`.
- integrar aba Produtos para consumir API primeiro com fallback explicito.

Escopo:
- sem banco real;
- sem CRUD completo;
- sem autenticacao nova.

Risco:
- dependencia de FastAPI/Uvicorn no Python local para subir o backend.

Status:
- backend minimo funcional para leitura de produtos via seed.

ETAPA V0.1.053B - REFATORACAO ARQUITETURAL BACK_END

Decisao:
- backend oficial unico em `01-mrp/back_end`.

Aplicado:
- estrutura temporaria `01-mrp/backend` removida apos migracao util;
- modulo Produtos ajustado como template por camadas:
  - route -> service -> repository/adapter -> domain/contracts -> seed;
- resposta de `GET /api/produtos` padronizada em `{ok, source, count, items}`.

Mantido:
- sem alteracao de seed e imagens;
- sem banco real;
- sem CRUD completo;
- sem autenticacao nova.
